// TODO: Page détail d'un élève (Director)
// Actions : getStudentById(schoolId, studentId), updateStudent(id, data),
//           updateEnrollmentStatus(data), archiveStudent(id),
//           getStudentPayments(schoolId, studentId), recordPayment(data)
// depuis @/lib/actions/students et @/lib/actions/payments
export default function StudentDetailPage({ params }: { params: { studentId: string } }) {
  return <div />;
}
