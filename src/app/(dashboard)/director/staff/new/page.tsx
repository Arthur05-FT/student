// TODO: Page création d'un membre du personnel (Director)
// Action : createStaff(data) depuis @/lib/actions/staff
// Envoie un email de bienvenue avec mot de passe temporaire
export default function NewStaffPage() {
  return <div />;
}
