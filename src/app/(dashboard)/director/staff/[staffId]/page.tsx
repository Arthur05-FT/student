// TODO: Page détail d'un membre du personnel (Director)
// Actions : getStaffById(staffId), updateStaff(id, data), toggleStaffActive(id),
//           assignTeacher(data), removeTeacherAssignment(id),
//           getLeaveRequests(schoolId), reviewLeaveRequest(data)
// depuis @/lib/actions/staff
export default function StaffDetailPage({ params }: { params: { staffId: string } }) {
  return <div />;
}
