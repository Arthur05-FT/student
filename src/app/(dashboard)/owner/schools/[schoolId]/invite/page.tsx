// TODO: Page invitation d'un directeur (Owner)
// Action : inviteDirector({ email, schoolId }) depuis @/lib/actions/schools
// Envoie un email avec un lien d'activation (acceptDirectorInvitation dans auth.ts)
export default function InviteDirectorPage({ params }: { params: { schoolId: string } }) {
  return <div />;
}
