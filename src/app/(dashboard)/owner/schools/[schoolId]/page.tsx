// TODO: Page détail d'un établissement (Owner)
// Actions : getSchoolById(schoolId), updateSchool(schoolId, data)
// depuis @/lib/actions/schools
export default function SchoolDetailPage({ params }: { params: { schoolId: string } }) {
  return <div />;
}
